{
  "name": "@makenew/serverless-nodejs",
  "version": "4.1.2",
  "description": "Package skeleton for a Node.js Serverless project on AWS Lambda.",
  "main": "dist/index.js",
  "module": "index.js",
  "sideEffects": false,
  "keywords": [
    "node"
  ],
  "homepage": "https://github.com/makenew/serverless-nodejs",
  "bugs": "https://github.com/makenew/serverless-nodejs/issues",
  "repository": "makenew/serverless-nodejs",
  "license": "MIT",
  "author": {
    "name": "Evan Sosenko",
    "email": "razorx@evansosenko.com"
  },
  "files": [
    "yarn.lock",
    "index.js",
    "lib",
    "handlers",
    "data",
    "dist",
    "docs"
  ],
  "scripts": {
    "test": "nyc ava",
    "test:update": "ava --update-snapshots",
    "test:watch": "ava --watch",
    "test:debug": "ava debug --break",
    "lint": "npm-run-all lint:*",
    "lint:prettier": "prettier --check --ignore-path .gitignore '**/*.(js|json|graphql)'",
    "lint:js": "eslint --ignore-path .gitignore --ext .js .",
    "clean": "del dist",
    "build": "npm-run-all --parallel build:*",
    "prebuild": "npm-run-all clean",
    "build:index": "babel --source-maps --out-dir dist index.js",
    "build:lib": "babel --source-maps --ignore='**/*.spec.js' --out-dir dist/lib lib",
    "build:handlers": "babel --source-maps --ignore='**/*.spec.js' --out-dir dist/handlers handlers",
    "serverless:package": "serverless package",
    "preversion": "npm-run-all --parallel lint test build serverless:package",
    "postversion": "git push --follow-tags",
    "deploy": "npm-run-all --serial deploy:domain deploy:stack",
    "deploy:domain": "serverless create_domain",
    "deploy:stack": "serverless deploy --aws-s3-accelerate",
    "release:staging": "true",
    "release:production": "true",
    "example": "babel-node examples",
    "example:inspect": "babel-node --inspect examples",
    "format": "npm-run-all --serial format:prettier format:js",
    "format:prettier": "prettier --write --ignore-path .gitignore '**/*.(js|json|graphql)'",
    "format:js": "eslint --ignore-path .gitignore --fix --ext .js .",
    "report": "nyc report"
  },
  "engines": {
    "node": ">=12.13.0"
  },
  "resolutions": {
    "aws-sdk": "2.771.0"
  },
  "dependencies": {
    "aws-sdk": "2.771.0",
    "source-map-support": "^0.5.9"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.1",
    "@babel/node": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/register": "^7.0.0",
    "@meltwater/examplr": "^5.0.0",
    "ava": "^3.3.0",
    "babel-loader": "^8.0.4",
    "babel-plugin-source-map-support": "^2.0.1",
    "del-cli": "^3.0.0",
    "eslint": "^7.4.0",
    "eslint-config-standard": "^16.0.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-node": "^11.0.0",
    "eslint-plugin-promise": "^4.2.1",
    "npm-run-all": "^4.1.2",
    "nyc": "^15.0.0",
    "prettier": "^2.0.2",
    "serverless": "^2.0.0",
    "serverless-domain-manager": "^5.0.0",
    "serverless-offline": "^6.1.2",
    "serverless-webpack": "^5.2.0",
    "webpack": "^5.3.2",
    "webpack-node-externals": "^2.5.0"
  }
}
